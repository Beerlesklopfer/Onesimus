# Console configuration for TLS-PSK mode (Bareos 18.2+)
# Copy to: /etc/bareos/bareos-dir.d/console/onesimus-psk.conf

Console {
  Name = onesimus-psk
  Password = "RDZTTAJE5pj5O/C2uOi01o8FRsqucVPolersd3AbXBs"

  CommandACL = *all*
  ClientAcl = *all*
  JobAcl = *all*
  StorageAcl = *all*
  ScheduleAcl = *all*
  PoolAcl = *all*
  FileSetAcl = *all*
  CatalogAcl = *all*

  # TLS-PSK mode: Uses password as pre-shared key
  TLS Enable = yes
  TLS Require = yes
  # No certificates needed for PSK mode
}
