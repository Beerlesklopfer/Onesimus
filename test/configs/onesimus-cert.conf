# Console configuration for Certificate TLS mode
# Copy to: /etc/bareos/bareos-dir.d/console/onesimus-cert.conf

Console {
  Name = onesimus-cert
  Password = "RDZTTAJE5pj5O/C2uOi01o8FRsqucVPolersd3AbXBs"

  CommandACL = *all*
  ClientAcl = *all*
  JobAcl = *all*
  StorageAcl = *all*
  ScheduleAcl = *all*
  PoolAcl = *all*
  FileSetAcl = *all*
  CatalogAcl = *all*

  # Certificate TLS mode
  TLS Enable = yes
  TLS Require = yes
  TLS Verify Peer = yes
  TLS CA Certificate File = /etc/bareos/ssl/bareos-ca.pem
  TLS Certificate = /etc/bareos/ssl/bareos-client.pem
  TLS Key = /etc/bareos/ssl/bareos-client.key
}
